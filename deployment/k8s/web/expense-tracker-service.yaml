apiVersion: v1
kind: Service
metadata:
  name: expense-tracker-service
spec:
  type: NodePort
  selector:
    app: expense-tracker
  ports:
    - protocol: TCP
      port: 8484
      targetPort: 8080
