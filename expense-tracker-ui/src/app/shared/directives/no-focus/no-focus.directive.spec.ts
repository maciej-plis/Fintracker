import { NoFocusDirective } from './no-focus.directive';

describe('NoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new NoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
