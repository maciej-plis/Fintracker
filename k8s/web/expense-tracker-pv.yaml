apiVersion: v1
kind: PersistentVolume
metadata:
  name: expense-tracker-persistent-volume
  labels:
    type: local
spec:
  claimRef:
    namespace: default
    name: expense-tracker-persistent-volume-claim
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/apps/expense-tracker"