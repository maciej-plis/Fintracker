properties:
  items:
    type: array
    items:
      $ref: '../../schema.yaml#/components/schemas/PurchaseDto'
  totalPages:
    type: integer
  totalItems:
    type: integer
    format: int64

required:
  - items
  - totalPages
  - totalItems